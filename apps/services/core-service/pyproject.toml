[tool.poetry.dependencies]
python = "^3.11"
fastapi = "^0.115.0"
uvicorn = { extras = ["standard"], version = "^0.30.0" }
sqlalchemy = "^2.0"
alembic = "^1.13"
motor = "^3.6.0"          # driver async do MongoDB
pydantic-settings = "^2.4" # se ainda não estiver presente

[tool.poetry.group.dev.dependencies]
pytest = "^8.0"
pytest-asyncio = "^0.24"